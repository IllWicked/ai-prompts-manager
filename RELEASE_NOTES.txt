Версия 4.3.1 Hotfix

ИСПРАВЛЕНИЯ:
- Паттерн «Матрица» больше не роняет FPS — анимация переведена на GPU-композитинг (transform вместо background-position), убраны glow-дубли с blur
- project-manager.py: update_app_version() теперь обновляет README.md и package.json
- project-manager.py: save_tab() больше не теряет поле version
- Добавлен package-lock.json для GitHub Actions
- Обновлена документация (числа строк, подсчёт функций, пропущенная команда)

Версия 4.3.0

ЧТО НОВОГО:
- Заметки на canvas: текстовые заметки для организации работы — перетаскивание, изменение размера, не влияют на сборку промпта
- Кастомизация оформления: выбор акцентного цвета (8 пресетов или свой) и фона холста (5 паттернов или своё изображение)
- Система маркеров языка: вставка маркеров {{lang}}, {{country}} и др. — при смене языка всё обновляется мгновенно
- Отмена отправки в Claude: можно прервать на любом этапе, понятные сообщения об ошибках на каждом шаге
- Инструкции блоков: иконка ⓘ в свёрнутых блоках с подсказкой при наведении, исправлена работа через контекстное меню
- Опциональные поля: скрытие/показ полей теперь работает корректно даже после редактирования текста
- Привязка к проекту Claude: автоматическое завершение через 4 часа, восстановление после сбоев
- Неактивные табы Claude приостанавливаются — меньше нагрузка на процессор и память
- Запуск приложения ускорен почти в 4 раза
- Undo/Redo: быстрее работает на больших промптах, мелкие правки группируются в одно действие
- Данные вкладок дублируются в файл — защита от потери при сбое
- Взаимодействие с Claude стало надёжнее: автоповтор при временных ошибках, адаптивные таймауты
- Диагностика селекторов Claude при запуске — приложение само обнаружит если Claude.ai изменит интерфейс
- Сброс приложения теперь сохраняет пользовательские настройки оформления